Run the following bash commands sequentially to commit and push all changes:
1. `git add .`
2. `git commit -m "$ARGUMENTS"` — use the arguments as the commit message (max 13 words)
3. `git push origin HEAD`

If no commit message is provided, ask the user for one before proceeding.
